/**
 * Test for ThePasswordHistoryResource.
 * Runs with mocha.
 */
'use strict'

const ThePasswordHistoryResource = require('../lib/ThePasswordHistoryResource')
const { ok, equal } = require('assert')

describe('the-password-history-resource', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
