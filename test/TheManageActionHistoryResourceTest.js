/**
 * Test for TheManageActionHistoryResource.
 * Runs with mocha.
 */
'use strict'

const TheManageActionHistoryResource = require('../lib/TheManageActionHistoryResource')
const { ok, equal } = require('assert')

describe('the-manage-action-history-resource', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
